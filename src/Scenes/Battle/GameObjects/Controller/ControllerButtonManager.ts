import { Component } from '@eva/eva.js';
import { CONTROLLER_ENUM } from '../../../../Enum/index';

export default class ControllerButtonManager extends Component {
    static componentName = 'BackgroundManager'; // 设置组件的名字

    type :CONTROLLER_ENUM


}
